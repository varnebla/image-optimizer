---
import { getLangFromUrl, useTranslations } from '@i18n/utils';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<section
  class="max-w-6xl mx-auto px-8 pb-16"
  aria-labelledby="faqs-title"
  id="faqs"
>
  <div class="max-w-3xl mx-auto">
    <h2
      id="faqs-title"
      class="text-3xl font-bold text-gray-800 mb-4 text-center"
    >
      {t('faq.title')}
    </h2>

    <p class="text-center text-gray-600 mb-12">
      {t('faq.subtitle')}
    </p>

    <div class="space-y-3">
      <details
        class="group border border-gray-200 rounded-lg p-4 hover:border-blue-300 hover:bg-blue-50 transition-all duration-200 cursor-pointer"
      >
        <summary
          class="flex items-center justify-between font-semibold text-gray-800 select-none"
        >
          {t('faq.q1')}
          <span
            class="text-gray-400 group-open:rotate-180 transition-transform duration-200"
          >
            ▼
          </span>
        </summary>
        <p class="text-gray-700 mt-4 leading-relaxed">
          {t('faq.a1')}
        </p>
      </details>

      <details
        class="group border border-gray-200 rounded-lg p-4 hover:border-blue-300 hover:bg-blue-50 transition-all duration-200 cursor-pointer"
      >
        <summary
          class="flex items-center justify-between font-semibold text-gray-800 select-none"
        >
          {t('faq.q2')}
          <span
            class="text-gray-400 group-open:rotate-180 transition-transform duration-200"
          >
            ▼
          </span>
        </summary>
        <p class="text-gray-700 mt-4 leading-relaxed">
          {t('faq.a2')}
        </p>
      </details>

      <details
        class="group border border-gray-200 rounded-lg p-4 hover:border-blue-300 hover:bg-blue-50 transition-all duration-200 cursor-pointer"
      >
        <summary
          class="flex items-center justify-between font-semibold text-gray-800 select-none"
        >
          {t('faq.q3')}
          <span
            class="text-gray-400 group-open:rotate-180 transition-transform duration-200"
          >
            ▼
          </span>
        </summary>
        <p class="text-gray-700 mt-4 leading-relaxed">
          {t('faq.a3')}
        </p>
      </details>

      <details
        class="group border border-gray-200 rounded-lg p-4 hover:border-blue-300 hover:bg-blue-50 transition-all duration-200 cursor-pointer"
      >
        <summary
          class="flex items-center justify-between font-semibold text-gray-800 select-none"
        >
          {t('faq.q4')}
          <span
            class="text-gray-400 group-open:rotate-180 transition-transform duration-200"
          >
            ▼
          </span>
        </summary>
        <p class="text-gray-700 mt-4 leading-relaxed">
          {t('faq.a4')}
        </p>
      </details>

      <details
        class="group border border-gray-200 rounded-lg p-4 hover:border-blue-300 hover:bg-blue-50 transition-all duration-200 cursor-pointer"
      >
        <summary
          class="flex items-center justify-between font-semibold text-gray-800 select-none"
        >
          {t('faq.q5')}
          <span
            class="text-gray-400 group-open:rotate-180 transition-transform duration-200"
          >
            ▼
          </span>
        </summary>
        <p class="text-gray-700 mt-4 leading-relaxed">
          {t('faq.a5')}
        </p>
      </details>

      <details
        class="group border border-gray-200 rounded-lg p-4 hover:border-blue-300 hover:bg-blue-50 transition-all duration-200 cursor-pointer"
      >
        <summary
          class="flex items-center justify-between font-semibold text-gray-800 select-none"
        >
          {t('faq.q6')}
          <span
            class="text-gray-400 group-open:rotate-180 transition-transform duration-200"
          >
            ▼
          </span>
        </summary>
        <p class="text-gray-700 mt-4 leading-relaxed">
          {t('faq.a6')}
        </p>
      </details>

      <details
        class="group border border-gray-200 rounded-lg p-4 hover:border-blue-300 hover:bg-blue-50 transition-all duration-200 cursor-pointer"
      >
        <summary
          class="flex items-center justify-between font-semibold text-gray-800 select-none"
        >
          {t('faq.q7')}
          <span
            class="text-gray-400 group-open:rotate-180 transition-transform duration-200"
          >
            ▼
          </span>
        </summary>
        <p class="text-gray-700 mt-4 leading-relaxed">
          {t('faq.a7')}
        </p>
      </details>

      <details
        class="group border border-gray-200 rounded-lg p-4 hover:border-blue-300 hover:bg-blue-50 transition-all duration-200 cursor-pointer"
      >
        <summary
          class="flex items-center justify-between font-semibold text-gray-800 select-none"
        >
          {t('faq.q8')}
          <span
            class="text-gray-400 group-open:rotate-180 transition-transform duration-200"
          >
            ▼
          </span>
        </summary>
        <p class="text-gray-700 mt-4 leading-relaxed">
          {t('faq.a8')}
        </p>
      </details>

      <details
        class="group border border-gray-200 rounded-lg p-4 hover:border-blue-300 hover:bg-blue-50 transition-all duration-200 cursor-pointer"
      >
        <summary
          class="flex items-center justify-between font-semibold text-gray-800 select-none"
        >
          {t('faq.q9')}
          <span
            class="text-gray-400 group-open:rotate-180 transition-transform duration-200"
          >
            ▼
          </span>
        </summary>
        <p class="text-gray-700 mt-4 leading-relaxed">
          {t('faq.a9')}
        </p>
      </details>

      <details
        class="group border border-gray-200 rounded-lg p-4 hover:border-blue-300 hover:bg-blue-50 transition-all duration-200 cursor-pointer"
      >
        <summary
          class="flex items-center justify-between font-semibold text-gray-800 select-none"
        >
          {t('faq.q10')}
          <span
            class="text-gray-400 group-open:rotate-180 transition-transform duration-200"
          >
            ▼
          </span>
        </summary>
        <p class="text-gray-700 mt-4 leading-relaxed">
          {t('faq.a10')}
        </p>
      </details>
    </div>
  </div>
</section>
